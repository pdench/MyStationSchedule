@model List<MyStationSchedule.Models.Routes>

@{
    ViewBag.Title = "Routes";
}

<h2>Routes</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Route
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.route_name)
        </td>

    </tr>
}

</table>
